# Staged MAME romsets (local copies; do not commit dumps)
roms/
*.zip

# Overlay build output
build/
bramble

# Guest firmware binaries (sync from MegaFlash build; see firmware/README.md)
firmware/*.uf2
firmware/*.elf

# MegaFlash SPI volumes + mirrored user config (large / machine-local)
flash/*.bin

# Apple IIc MegaFlash ROM + reference HDV
iic.bin
A2DeskTop.hdv
*.hdv
*.po

# MAME local runtime (created beside the launcher)
cfg/
nvram/
snap/
inp/
diff/
hi/
.run/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
